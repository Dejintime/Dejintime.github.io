# 博客本地环境：通俗版工作流

这份文档用**尽量少术语**的方式，帮你把「Docker、镜像、容器、挂载」和**日常该敲什么命令**串成一条线。技术细节仍以 [README.md](./README.md) 为准。

---

## 你先记住这一句话

> **Docker 负责给你一台「装着对版 Ruby 和工具的迷你 Linux」；你的博客文件还在 Mac 的文件夹里，只是「借给」这台迷你机用一下。**

你改文章、改样式，改的还是自己磁盘上的文件；跑 Jekyll 的，是容器里的程序。

---

## 用一个比喻分清三件事（最容易混的地方）

想象你在**家里书桌**写博客：

| 说法 | 比喻成什么 | 在本项目里大致对应 |
|------|------------|----------------------|
| **镜像（Image）** | 一个**工具箱**：里面有合适的 Ruby、编译器、Bundler 版本 | `Dockerfile` 搭出来的那个「模板」 |
| **容器（Container）** | 把工具箱打开、**真正开工的那一次** | 你执行 `docker compose up` 后跑起来的那个进程 |
| **挂载（卷）** | 把你的**作业本**（博客文件夹）放到桌上，和工具箱一起用 | `docker-compose.yml` 里的「当前文件夹 → 容器里的 `/srv/jekyll`」 |

所以：

- **不是**把整站代码「拷进镜像里再开发」（那样你每次改代码都要重建镜像）。
- **而是**：镜像只解决「环境对不对」；代码始终在你熟悉的那个项目文件夹里。

---

## 两条线：只准备「工具箱」vs 真正「写博客、预览」

### 线 A：准备工具箱（偶尔做一次，或改了 Dockerfile 再做）

```bash
docker compose build
```

含义：根据 `Dockerfile` 做一块「带 Ruby 3.2、系统库、Bundler 2.6.7、启动脚本」的镜像。  
**这一步一般不会帮你把 Jekyll 所有小依赖都锁进镜像里**（那样和改代码不同步）；具体 gem 是下一步容器启动时再装的。

### 线 B：日常写博客、看网页效果（你最常做的）

```bash
docker compose up
```

含义：

1. 用上面的镜像**开一个容器**。
2. 把你的项目文件夹**挂到**容器里的 `/srv/jekyll`。
3. 自动执行：装好依赖（到项目里的 `vendor/bundle`）→ 启动 `jekyll serve`。
4. 你在浏览器打开 **http://localhost:4000** 就能预览。

关掉：终端里 `Ctrl+C`，或另开终端 `docker compose down`。

---

## 「我敲这条命令时，谁先谁后？」（简单时间线）

### `docker compose up` 时（心里可以按这个顺序理解）

1. **有镜像吗？** 没有就先 build（或你事先 `docker compose build` 过）。
2. **启动容器**，把本机当前目录挂到 `/srv/jekyll`。
3. **入口脚本**（`docker-entrypoint.sh`）跑起来：
   - 说：gem 都装到本项目的 `vendor/bundle`（避免和 Mac 自带 Ruby 搅在一起）。
   - 执行 `bundle install`（按 `Gemfile.lock` 装齐 Jekyll 等）。
   - 再执行 `jekyll serve`，并把 4000 端口映射到你本机。

所以你看到第一次启动会慢一点，多半是在装依赖；以后再开，有缓存会快很多。

### `docker compose run --rm jekyll build` 时

和 `up` 类似，也是进同一套环境，但最后一步不是「长期开着网站」，而是**生成一遍静态文件到 `_site/`**，然后容器退出。适合「我只要产物，不预览」。

---

## 和「在 Cursor 里开 Dev Container」是什么关系？

可以当成**同一种工具箱、两种开门方式**：

| 方式 | 你平时怎么用 | 适合什么时候 |
|------|----------------|----------------|
| **Docker Compose** | 终端里 `docker compose up`，浏览器看 | 就想快速预览，不关心编辑器是不是在容器里 |
| **Dev Container** | 编辑器菜单里「在 Dev Container 中打开」 | 希望终端、Ruby、扩展都在容器里，和团队环境更一致 |

底层都用仓库里的 **`Dockerfile`**，不会两套互相打架。区别主要是：**你从终端进门还是从编辑器进门**。

---

## 仓库里几个文件，各自一句话

| 文件 | 一句话 |
|------|--------|
| `Dockerfile` | 定义「工具箱」里装什么系统、什么 Ruby、什么 Bundler |
| `docker-compose.yml` | 定义「怎么开门」：挂哪个文件夹、映射哪个端口、默认跑预览还是别的事 |
| `docker-entrypoint.sh` | 门打开后**自动执行的几步**：进目录、装 gem、再启动 Jekyll（或 build） |
| `Gemfile` / `Gemfile.lock` | 博客依赖清单和**精确版本**，Bundler 按它装 |
| `vendor/bundle/` | 装下来的 gem 放这里（一般不用提交到 Git） |
| `.devcontainer/devcontainer.json` | 告诉编辑器：用同一个 `Dockerfile` 搭开发容器 |

---

## 容易产生的误解（对照一下）

- **「我是不是在 Docker 里改代码？」**  
  文件还是在你 Mac 的项目目录里；容器只是**读同一份文件**来编译和预览。

- **「为什么不在 Dockerfile 里直接 bundle install？」**  
  代码是挂进去的，依赖又常随 `Gemfile` 变，把「装依赖」放在**每次启动容器时**更简单、不容易和挂载冲突。（详见 README 技术说明。）

- **「关掉 Docker 我的博客会丢吗？」**  
  不会。文章和配置都在你的文件夹里；关掉只是停了预览服务。`vendor/bundle` 还在的话，下次启动会省不少时间。

---

## 想再细一点？

请继续看 [README.md](./README.md) 里的「使用 Docker 构建与运行环境的详细说明」和「常用启动命令」两节。
